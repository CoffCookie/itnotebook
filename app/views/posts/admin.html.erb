

  <p><%= link_to("新規投稿","new") %></p>

  <% @posts.each do |post| %>
  <%= post.id %>
  <%= link_to truncate(post.content, length:15), edit_post_path(post) %>
  <%= link_to("削除","destroy/#{post.id}",{method:"post"}) %>
  <br>
  <% end %>

  <%= link_to("ログアウト","/users/logout",{method: :post}) %>
